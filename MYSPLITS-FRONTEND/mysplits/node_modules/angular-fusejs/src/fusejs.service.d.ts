import * as Fuse from 'fuse.js';
import FuseOptions = Fuse.FuseOptions;
export interface AngularFusejsOptions extends FuseOptions {
    supportHighlight?: boolean;
    fusejsHighlightKey?: string;
    fusejsScoreKey?: string;
    minSearchTermLength?: number;
    maximumScore?: number;
}
export declare class FusejsService {
    private defaultOptions;
    searchList(list: Array<any>, searchTerms: string, options?: AngularFusejsOptions): any[];
    private deepClone(o);
    private handleHighlight(result, options);
}

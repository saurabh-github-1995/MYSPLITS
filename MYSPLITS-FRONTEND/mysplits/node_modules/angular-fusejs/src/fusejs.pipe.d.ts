import { PipeTransform } from '@angular/core';
import { FusejsService, AngularFusejsOptions } from './fusejs.service';
export declare class FusejsPipe implements PipeTransform {
    private FusejsService;
    constructor(FusejsService: FusejsService);
    transform(elements: Array<Object>, searchTerms: string, options?: AngularFusejsOptions): any[];
}
